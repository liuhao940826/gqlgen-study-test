
#自定义的user模块参数
type Todo {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}

type User implements Person{
  id:ID!
  name:String!
  age:Int64!
  "用户的地址"
  address:String
}

input NewTodo {
  text: String!
  userId: String!
}