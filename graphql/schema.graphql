# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query{
  todos: [Todo!]!
  players: [Player!]!
}

type Mutation {
  createTodo(input: NewTodo!): Todo!
  createPlayer(input: RegisterUser!): Player!
}